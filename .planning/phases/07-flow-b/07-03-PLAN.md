---
phase: 07-flow-b
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified: []
autonomous: false
requirements:
  - AGTWIZ-01
  - AGTWIZ-02
  - AGTWIZ-03
  - AGTWIZ-04
  - AGTWIZ-05

must_haves:
  truths:
    - "Full test suite passes with no regressions"
    - "TypeScript compilation produces no errors"
    - "Operator can run clisbot setup agent in a live terminal and see each wizard screen"
    - "Binary check with a genuinely missing CLI shows install instructions in the real terminal"
    - "After wizard completes, the runtime starts or confirms reload"
  artifacts:
    - path: "src/control/setup/setup-agent.ts"
      provides: "Verified Flow B wizard — green tests + type-clean + manually confirmed"
      exports: ["runAgentWizard"]
    - path: "test/control/setup/setup-agent.test.ts"
      provides: "All AGTWIZ tests green"
      exports: []
  key_links:
    - from: "bun run check"
      to: "test suite + typecheck + lint"
      via: "bun.toml scripts"
      pattern: "bun run check"
---

<objective>
Run the full verification gate for Phase 7: full `bun run check` must pass with zero regressions, then a manual live terminal test confirms the wizard runs correctly end-to-end.

Purpose: Confirm that the Phase 7 deliverables are production-ready — unit tests alone do not verify real readline interaction, actual binary detection, or real runtime reload behaviour.
Output: Phase 7 verified complete; ROADMAP.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-flow-b/07-VALIDATION.md
@.planning/phases/07-flow-b/07-01-SUMMARY.md
@.planning/phases/07-flow-b/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full bun run check gate</name>
  <files></files>

  <read_first>
    - .planning/phases/07-flow-b/07-02-SUMMARY.md — confirm Wave 1 completed successfully before running the gate
    - CLAUDE.md section "Command Baseline" — confirms `bun run check` is the correct full gate command; do not invent alternative commands
  </read_first>

  <action>
Run the full check suite in sequence. Do not skip any step even if a prior step passes:

1. Targeted test run first (fast feedback):
   `bun test test/control/setup/setup-agent.test.ts`
   All 5 AGTWIZ tests must be green.

2. Full suite:
   `bun test`
   No regressions across the entire test suite.

3. Typecheck:
   `bunx tsc --noEmit`
   Zero errors. If errors exist in setup-agent.ts, fix before claiming task done.

4. Full gate:
   `bun run check`
   Must exit 0.

If any step fails:
- Diagnose from the error output
- Fix in src/control/setup/setup-agent.ts or test/control/setup/setup-agent.test.ts
- Re-run only the failing step, then re-run bun run check to confirm clean
- Do not claim Task 1 done until bun run check exits 0
  </action>

  <verify>
    <automated>cd /home/brewuser/clisbot && bun run check 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `bun run check` exits 0
    - `bun test test/control/setup/setup-agent.test.ts` exits 0 with 5 passing tests
    - `bun test` exits 0 (no regressions)
    - `bunx tsc --noEmit` exits 0
    - No test in any file that was previously passing now fails
  </acceptance_criteria>

  <done>
    bun run check exits 0. All 5 AGTWIZ tests green. No regressions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live terminal integration test</name>

  <read_first>
    - .planning/phases/07-flow-b/07-VALIDATION.md — Manual-Only Verifications section; confirms which behaviors require live terminal
    - src/control/setup/setup-agent.ts — review the success message text to know what exact strings to expect in the terminal
  </read_first>

  <what-built>
    Wave 0 created the RED test scaffold; Wave 1 implemented setup-agent.ts and turned all 5 tests GREEN; Task 1 of this plan confirmed the full gate passes. The wizard is now ready for live terminal validation.
  </what-built>

  <how-to-verify>
    Run each scenario in order. All three must behave correctly.

    **Scenario A — Channel summary display (AGTWIZ-01):**
    1. Open an interactive terminal (not piped, not CI)
    2. Run: `bun run start` — note whether the runtime is running or stopped
    3. If running, stop it: `bun run status && clisbot stop` (wizard requires daemon not running)
    4. Run: `CLISBOT_CONFIG_PATH=/tmp/test-agent-wizard.json bun run src/control/setup/setup-agent.ts` (or the registered CLI command if wired)
       Alternative: `bun run -e "import('./src/control/setup/setup-agent.ts').then(m => m.runAgentWizard({ configPath: '/tmp/test-agent-wizard.json' }))"`
    5. Verify: First non-header output block shows "Configured Channels:" label (even if empty)
    6. Press Ctrl+C to exit

    **Scenario B — Missing binary install instructions (AGTWIZ-02):**
    1. Confirm a tool that is NOT installed: `which nonexistent-cli-tool` — should return nothing
    2. Repeat step 4 from Scenario A but type a supported tool name that is not installed (e.g., 'gemini' if not installed)
    3. Verify: Wizard prints "Binary not found: gemini" and shows an install command, then re-prompts for CLI selection
    4. Press Ctrl+C to exit

    **Scenario C — Full flow (AGTWIZ-03, AGTWIZ-04, AGTWIZ-05):**
    1. Repeat step 4 from Scenario A
    2. Select a CLI that IS installed on your system (e.g., 'claude' or 'codex')
    3. When prompted for bot type, select '1' (personal-assistant) — verify descriptions are shown
    4. At channel linking: verify at least one channel prompt appears (even "No channels configured" is acceptable)
    5. At review screen: verify CLI Tool, Bot Type, Channels summary is displayed
    6. At "Proceed?": type 'n' to cancel (do not write config in test)
    7. Verify: "Setup cancelled." appears and wizard exits cleanly with no error

    **Expected terminal output at each step:**
    - Scenario A: "=== clisbot setup agent ===" header, then "Configured Channels:" block
    - Scenario B: "Binary not found: <tool>" + "Install: <command>" text visible
    - Scenario C: "1. personal-assistant — single operator, focused workspace" visible before prompt; review block shows "REVIEW AGENT CONFIGURATION"; "Setup cancelled." on 'n'
  </how-to-verify>

  <resume-signal>
    Type "approved" if all three scenarios behave as described.
    Or describe which scenario failed and what was shown instead.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md to mark Phase 7 complete</name>
  <files>.planning/ROADMAP.md</files>

  <read_first>
    - .planning/ROADMAP.md — find the Phase 7 section; read the current Plans: TBD placeholder before writing
  </read_first>

  <action>
Update .planning/ROADMAP.md Phase 7 section:

1. Change `**Plans**: TBD` to `**Plans**: 3 plans`

2. Replace the `Plans:\n- TBD` list with:
```
Plans:
- [x] 07-01-PLAN.md — Wave 0: failing test scaffold for Flow B wizard (AGTWIZ-01 through AGTWIZ-05, RED state)
- [x] 07-02-PLAN.md — Wave 1: implement setup-agent.ts Flow B wizard (GREEN)
- [x] 07-03-PLAN.md — Wave 2: full bun run check gate + manual integration checkpoint
```

3. Update the progress table row for Phase 7:
   - Plans Complete: `3/3`
   - Status: `Complete`
   - Completed: `2026-05-28`

Do not modify any other section of ROADMAP.md.
  </action>

  <verify>
    <automated>cd /home/brewuser/clisbot && grep -A 8 "Phase 7: Flow B" .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - grep '07-01-PLAN.md\|07-02-PLAN.md\|07-03-PLAN.md' .planning/ROADMAP.md returns 3 lines each with [x]
    - grep 'Phase 7.*Complete\|7.*3/3' .planning/ROADMAP.md returns at least 1 line
    - No other Phase sections were modified (verify by checking Phase 5, 6, 8 entries still match their pre-existing content)
  </acceptance_criteria>

  <done>
    ROADMAP.md Phase 7 section shows 3/3 plans complete with all three plan filenames checked off.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live terminal → wizard process | Real stdin/stdout; manual verification catches TTY-specific issues unit tests cannot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Config file mid-write crash | mitigate | addAgentToEditableConfig uses writeEditableConfig; withWizardCleanup removes .tmp on SIGINT — covered by Phase 5 infrastructure |
| T-07-02 | Tampering | Partial agent config on SIGINT | mitigate | withWizardCleanup SIGINT handler exits before write completes; verified in Scenario C (Ctrl+C before "Proceed? y") |
| T-07-03 | Tampering | Overwriting existing agent config | mitigate | addAgentToEditableConfig calls ensureAgentMissing() — throws if agent 'default' already exists; prevents accidental overwrite |
</threat_model>

<verification>
- `bun run check` exits 0
- Manual checkpoint approved by operator
- ROADMAP.md shows Phase 7 complete with 3/3 plans
</verification>

<success_criteria>
- bun run check passes (typecheck + full test suite + lint)
- All 5 AGTWIZ tests green
- No regressions in Phase 5 or Phase 6 tests
- Human approves all three live terminal scenarios
- ROADMAP.md updated to reflect Phase 7 complete
</success_criteria>

<output>
After completion, create .planning/phases/07-flow-b/07-03-SUMMARY.md
</output>
