---
phase: 07-flow-b
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - test/control/setup/setup-agent.test.ts
autonomous: true
requirements:
  - AGTWIZ-01
  - AGTWIZ-02
  - AGTWIZ-03
  - AGTWIZ-04
  - AGTWIZ-05

must_haves:
  truths:
    - "Running the test file produces exactly 5 failing tests (one per AGTWIZ requirement)"
    - "Each failing test references the correct AGTWIZ ID in its name"
    - "Tests use the same readline mock pattern as setup-channels.test.ts"
    - "execSync is mocked so tests control binary existence without PATH manipulation"
  artifacts:
    - path: "test/control/setup/setup-agent.test.ts"
      provides: "RED test scaffold for AGTWIZ-01 through AGTWIZ-05"
      exports: []
  key_links:
    - from: "test/control/setup/setup-agent.test.ts"
      to: "src/control/setup/setup-agent.ts"
      via: "import { runAgentWizard }"
      pattern: "runAgentWizard"
---

<objective>
Create the failing (RED) test scaffold for the Flow B agent wizard. All five AGTWIZ tests must exist and fail before any implementation is written.

Purpose: TDD gate — Wave 1 implementation has a concrete test contract to satisfy rather than writing tests after the fact.
Output: test/control/setup/setup-agent.test.ts with AGTWIZ-01 through AGTWIZ-05 in RED state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-flow-b/07-RESEARCH.md
@.planning/phases/07-flow-b/07-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/config/runtime/agent-tool-presets.ts:
```typescript
export const SUPPORTED_AGENT_CLI_TOOLS = ["codex", "claude", "gemini", "pi"] as const
export type AgentCliToolId = (typeof SUPPORTED_AGENT_CLI_TOOLS)[number]
export const SUPPORTED_BOOTSTRAP_MODES = ["personal-assistant", "team-assistant"] as const
export type AgentBootstrapMode = (typeof SUPPORTED_BOOTSTRAP_MODES)[number]
```

From src/control/setup/setup-agent.ts (does NOT exist yet — this import will fail RED):
```typescript
export async function runAgentWizard(options?: { configPath?: string }): Promise<void>
```

From test/control/setup/setup-channels.test.ts (readline mock pattern to copy exactly):
```typescript
function mockReadline(responses: string[]): void {
  const queue = [...responses]
  mock.module('node:readline', () => ({
    createInterface: mock(() => ({
      question(_prompt: string, callback: (answer: string) => void): void {
        const answer = queue.shift() ?? ''
        Promise.resolve().then(() => callback(answer))
      },
      close: mock(() => undefined),
      on: mock((_event: string, _handler: unknown) => undefined),
    })),
  }))
}
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED test scaffold for AGTWIZ-01 through AGTWIZ-05</name>
  <files>test/control/setup/setup-agent.test.ts</files>

  <read_first>
    - test/control/setup/setup-channels.test.ts — copy the exact beforeEach/afterEach structure, readline mock helper, and console.log capture pattern; do not invent a new structure
    - src/config/runtime/agent-tool-presets.ts lines 1-7 — confirm SUPPORTED_AGENT_CLI_TOOLS and SUPPORTED_BOOTSTRAP_MODES values
    - .planning/phases/07-flow-b/07-PATTERNS.md — test scaffold section for execSync mock helper and per-test response queues
  </read_first>

  <behavior>
    - AGTWIZ-01 test: calls runAgentWizard with a temp configPath that has no config file; expects output to match /configured channels|channels:/i before any CLI selection prompt appears
    - AGTWIZ-02 test: mocks execSync to throw ENOENT for 'which codex'; feeds 'codex' as CLI selection; expects output to match /not found|install/i and /codex/
    - AGTWIZ-03 test: mocks execSync to succeed for 'which claude'; feeds 'claude' as CLI, then '1' for bot type; expects output to match /personal|team|bot type/i
    - AGTWIZ-04 test: mocks execSync to succeed for 'which claude'; feeds CLI + bot type + channel confirm answers; expects output to match /link to|channels/i
    - AGTWIZ-05 test: mocks execSync and startDetachedRuntime; feeds full wizard inputs; expects output to match /complete|success|next step/i
    - All five tests import from '../../../src/control/setup/setup-agent.ts' (file does not exist — causes import failure = RED)
  </behavior>

  <action>
Create test/control/setup/setup-agent.test.ts using the following structure (2-space indent, no semicolons, single quotes, LF):

1. Imports: `afterEach, beforeEach, describe, expect, mock, test` from 'bun:test'; `existsSync, mkdtempSync, rmSync` from 'node:fs'; `tmpdir` from 'node:os'; `join` from 'node:path'; `runAgentWizard` from '../../../src/control/setup/setup-agent.ts' (this WILL fail until Wave 1)

2. mockReadline helper: identical to setup-channels.test.ts — FIFO queue, mock.module('node:readline'), createInterface returns { question, close, on }

3. mockBinaryExists helper: mock.module('node:child_process') with execSync that for `which <binaryName>` either returns '' (found) or throws `Object.assign(new Error('not found'), { code: 'ENOENT' })` (missing); throws for unexpected commands

4. beforeEach: save and delete CLISBOT_CONFIG_PATH from env; mkdtempSync with prefix 'clisbot-agent-test-'; capture console.log into logOutput[]

5. afterEach: restore CLISBOT_CONFIG_PATH; restore console.log; rmSync(tempDir); mock.restore()

6. Five tests (all inside `describe('setup-agent wizard', ...)`):

   test('AGTWIZ-01: displays configured channels before CLI selection', ...):
     - mockReadline(['n']) — single 'n' to abort at first prompt after channel summary
     - NO mockBinaryExists (wizard exits before binary check if user aborts)
     - await runAgentWizard({ configPath: join(tempDir, 'clisbot.json') })
     - expect(logOutput.join('\n')).toMatch(/configured channels|channels:/i)

   test('AGTWIZ-02: shows install instructions when binary is missing', ...):
     - mockBinaryExists('codex', false)
     - mockReadline(['codex', 'n']) — select codex (triggers binary check fail), then 'n' to cancel
     - await runAgentWizard({ configPath: join(tempDir, 'clisbot.json') })
     - expect output to match /not found|install/i
     - expect output to contain 'codex'

   test('AGTWIZ-03: displays bot-type descriptions before selection', ...):
     - mockBinaryExists('claude', true)
     - mockReadline(['claude', 'n']) — select claude (binary found), then 'n' at bot type or later
     - await runAgentWizard({ configPath: join(tempDir, 'clisbot.json') })
     - expect output to match /personal.assistant|team.assistant|bot type/i

   test('AGTWIZ-04: shows channel linking options', ...):
     - mockBinaryExists('claude', true)
     - mockReadline(['claude', '1', 'n']) — CLI, bot type, then 'n' at channel link or confirm
     - await runAgentWizard({ configPath: join(tempDir, 'clisbot.json') })
     - expect output to match /link to|channels/i

   test('AGTWIZ-05: shows success message after agent config is written', ...):
     - mockBinaryExists('claude', true)
     - mock.module('../../../src/control/runtime/runtime-process.ts', ...) returning { startDetachedRuntime: mock(() => Promise.resolve({ pid: 999 })), getRuntimeStatus: mock(() => Promise.resolve({ running: false })), ensureConfigFile: mock((p: string) => Promise.resolve({ configPath: p })) }
     - mockReadline(['claude', '1', 'y', 'y']) — CLI, bot type, link channel (y), confirm (y)
     - await runAgentWizard({ configPath: join(tempDir, 'clisbot.json') })
     - expect output to match /complete|success|next step/i

Note: AGTWIZ-02 test — mockReadline queue must handle the retry loop. After binary fails, wizard re-prompts. Feed ['codex', 'n'] — first 'codex' triggers binary fail message, 'n' should cancel the wizard (implement cancel-on-invalid or let wizard loop; test verifies message not full flow).
  </action>

  <verify>
    <automated>cd /home/brewuser/clisbot && bun test test/control/setup/setup-agent.test.ts 2>&1 | head -40</automated>
  </verify>

  <acceptance_criteria>
    - File exists at test/control/setup/setup-agent.test.ts
    - Running `bun test test/control/setup/setup-agent.test.ts` exits non-zero (tests fail = RED state)
    - Output contains exactly 5 test names matching AGTWIZ-01, AGTWIZ-02, AGTWIZ-03, AGTWIZ-04, AGTWIZ-05
    - Failure reason is import error for src/control/setup/setup-agent.ts (file not found), NOT a syntax error in the test file itself — verify by checking error message
    - grep 'AGTWIZ-0[1-5]' test/control/setup/setup-agent.test.ts outputs 5 lines
    - grep 'mockReadline\|mockBinaryExists' test/control/setup/setup-agent.test.ts shows both helpers present
    - File uses 2-space indent, no semicolons, single quotes (verify with: grep '	' test/control/setup/setup-agent.test.ts returns empty)
  </acceptance_criteria>

  <done>
    Five failing tests exist in test/control/setup/setup-agent.test.ts. Each test name contains its AGTWIZ ID. The import of runAgentWizard from the not-yet-created setup-agent.ts is the sole cause of failure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → src | Tests import production modules; mocking isolates side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Config file mid-write crash | mitigate | writeEditableConfigAtomic (temp+rename) — Wave 1 enforces this |
| T-07-02 | Tampering | Partial agent config on SIGINT | mitigate | withWizardCleanup SIGINT handler (Phase 5) — Wire in Wave 1 |
| T-07-03 | Tampering | Operator overwrites existing agent without review | mitigate | Review screen before write + ensureAgentMissing in addAgentToEditableConfig |
</threat_model>

<verification>
Run `bun test test/control/setup/setup-agent.test.ts` — must exit non-zero with 5 failing tests.
Do NOT run `bun test` (full suite) until Wave 2 — RED state is correct at Wave 0.
</verification>

<success_criteria>
- test/control/setup/setup-agent.test.ts exists and contains 5 tests
- All 5 tests fail (RED) because src/control/setup/setup-agent.ts does not exist
- Test file has no syntax errors (only import failure)
- mockReadline and mockBinaryExists helpers are present and follow the exact pattern from setup-channels.test.ts / 07-PATTERNS.md
</success_criteria>

<output>
After completion, create .planning/phases/07-flow-b/07-01-SUMMARY.md
</output>
