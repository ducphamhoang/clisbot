---
phase: 02-runner-session-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/runtime/agent-tool-presets.ts
  - test/runner-service.integration.test.ts
autonomous: true
requirements: [RUNNER-01, RUNNER-02, RUNNER-03, RUNNER-04, RUNNER-05, SESSION-01, SESSION-02, SESSION-03, SCHEMA-03]

must_haves:
  truths:
    - "Setting cli: \"pi\" in agent config causes clisbot to spawn pi with --session {uuid} via the tmux runner"
    - "Pi session resume passes --session {uuid} plus --dangerously-skip-permissions without creating a new session"
    - "inferAgentCliToolId returns \"pi\" for the string \"pi\""
    - "SUPPORTED_AGENT_CLI_TOOLS includes \"pi\" so AgentCliToolId type covers it"
    - "Pi startup ready detection uses the escape interrupt help bar pattern"
  artifacts:
    - path: "src/config/runtime/agent-tool-presets.ts"
      provides: "Pi entry in DEFAULT_AGENT_TOOL_TEMPLATES with explicit session mode, startup ready pattern, startup blockers, newSessionCommand"
      contains: "\"pi\""
    - path: "test/runner-service.integration.test.ts"
      provides: "Regression tests for pi template fields and inferAgentCliToolId"
      contains: "pi"
  key_links:
    - from: "SUPPORTED_AGENT_CLI_TOOLS"
      to: "DEFAULT_AGENT_TOOL_TEMPLATES"
      via: "Record<AgentCliToolId, AgentToolTemplate> — must include pi in both"
      pattern: "\"pi\""
    - from: "DEFAULT_AGENT_TOOL_TEMPLATES[\"pi\"].sessionId.create.mode"
      to: "RunnerService.launchNewSession()"
      via: "buildRunnerFromToolTemplate passes sessionId config through; RunnerService reads create.mode to decide UUID generation"
      pattern: "explicit"
---

<objective>
Register pi as a supported agent CLI tool by adding its entry to SUPPORTED_AGENT_CLI_TOOLS, DEFAULT_AGENT_TOOL_TEMPLATES, and inferAgentCliToolId in agent-tool-presets.ts.

Purpose: This is the primary integration point. Once pi is declared in the template, the entire tmux runner pipeline (session creation, startup detection, resume, active timer) works without further code changes — it is template-driven.

Output: agent-tool-presets.ts with pi entry; regression tests covering pi template shape and CLI tool recognition.
</objective>

<execution_context>
@/home/brewuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/brewuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/brewuser/clisbot/.planning/PROJECT.md
@/home/brewuser/clisbot/.planning/ROADMAP.md
@/home/brewuser/clisbot/.planning/STATE.md
@/home/brewuser/clisbot/.planning/phases/01-schema-precondition/01-01-SUMMARY.md

<interfaces>
<!-- Extracted from src/config/runtime/agent-tool-presets.ts — read this file before editing -->

Line 1: export const SUPPORTED_AGENT_CLI_TOOLS = ["codex", "claude", "gemini"] as const;
Line 2: export type AgentCliToolId = (typeof SUPPORTED_AGENT_CLI_TOOLS)[number];

Lines 7-39: AgentToolTemplate type — all fields pi must satisfy (command, startupOptions,
  trustWorkspace, startupDelayMs, startupRetryCount, startupRetryDelayMs,
  startupReadyPattern?, startupBlockers?, promptSubmitDelayMs, newSessionCommand?,
  sessionId: { create: {mode, args}, capture: {mode, statusCommand, pattern, timeoutMs, pollIntervalMs}, resume: {mode, command?, args} })

Lines 41-43:
  const SESSION_ID_PATTERN = "\\b[0-9a-fA-F]{8}-...-[0-9a-fA-F]{12}\\b";
  export const INTERACTIVE_CLI_STARTUP_DELAY_MS = 60_000;

Lines 45-157: DEFAULT_AGENT_TOOL_TEMPLATES — Record<AgentCliToolId, AgentToolTemplate>
  - codex: lines 46-83 (mode: "runner", capture: "status-command", codex-specific)
  - claude: lines 84-114 (mode: "explicit", args: ["--session-id", "{sessionId}"], capture: "off") — CLOSEST ANALOG
  - gemini: lines 115-157 (has startupBlockers, startupReadyPattern)
  Pi goes after gemini entry, before closing brace of the Record.

Lines 173-235: buildRunnerFromToolTemplate — pi uses the default (non-codex) branch; no changes needed.

Lines 237-256: inferAgentCliToolId — add "pi" case before final return null (after gemini case at line 252).

Note on buildRunnerFromToolTemplate default branch resume args (lines 229-233):
  resume: { ...template.sessionId.resume, args: ["--resume", "{sessionId}", ...options] }
  This auto-builds resume args from options. Pi template resume.args will be overridden by this.
  So pi template's resume.args value does not matter for the built result — but must still be
  provided to satisfy the type. Set to [] or the explicit args; the function rewrites it anyway.
  Verify the built resume args match expected: ["--resume", "{sessionId}", "--dangerously-skip-permissions"].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pi to SUPPORTED_AGENT_CLI_TOOLS, DEFAULT_AGENT_TOOL_TEMPLATES, and inferAgentCliToolId</name>
  <files>src/config/runtime/agent-tool-presets.ts, test/runner-service.integration.test.ts</files>
  <behavior>
    - Test 1: SUPPORTED_AGENT_CLI_TOOLS includes "pi"
    - Test 2: DEFAULT_AGENT_TOOL_TEMPLATES["pi"] exists and has command: "pi"
    - Test 3: DEFAULT_AGENT_TOOL_TEMPLATES["pi"].sessionId.create.mode === "explicit" (per SESSION-01)
    - Test 4: DEFAULT_AGENT_TOOL_TEMPLATES["pi"].sessionId.capture.mode === "off" (per SESSION-02)
    - Test 5: DEFAULT_AGENT_TOOL_TEMPLATES["pi"].sessionId.create.args deepEquals ["--session", "{sessionId}"] (per RUNNER-02)
    - Test 6: DEFAULT_AGENT_TOOL_TEMPLATES["pi"].sessionId.resume.mode === "command" (per SESSION-03)
    - Test 7: DEFAULT_AGENT_TOOL_TEMPLATES["pi"].newSessionCommand === "/new" (per RUNNER-01 + Phase 1 SCHEMA-01)
    - Test 8: DEFAULT_AGENT_TOOL_TEMPLATES["pi"].startupReadyPattern matches "escape interrupt" text (per RUNNER-04)
    - Test 9: inferAgentCliToolId("pi") returns "pi" (per RUNNER-05)
    - Test 10: inferAgentCliToolId("PI") returns "pi" (case-insensitive, consistent with existing function behavior)
    - Test 11: buildRunnerFromToolTemplate("pi", DEFAULT_AGENT_TOOL_TEMPLATES["pi"], undefined).args deepEquals ["--dangerously-skip-permissions"]
    - Test 12: buildRunnerFromToolTemplate("pi", template, undefined).sessionId.resume.args deepEquals ["--resume", "{sessionId}", "--dangerously-skip-permissions"]
  </behavior>
  <action>
    Write tests RED first in test/runner-service.integration.test.ts — add a new describe block
    "pi runner template" after the existing "newSessionCommand defaults" block.

    Then implement in src/config/runtime/agent-tool-presets.ts:

    1. Line 1: add "pi" to SUPPORTED_AGENT_CLI_TOOLS array:
       ["codex", "claude", "gemini", "pi"]

    2. After the gemini entry (after line 157 closing brace of gemini, before the closing `}`
       of DEFAULT_AGENT_TOOL_TEMPLATES), add the pi entry:

       pi: {
         command: "pi",
         startupOptions: ["--dangerously-skip-permissions"],
         trustWorkspace: true,
         startupDelayMs: INTERACTIVE_CLI_STARTUP_DELAY_MS,
         startupRetryCount: 2,
         startupRetryDelayMs: 1000,
         startupReadyPattern: "(?:^|\\s)escape\\s+interrupt(?:\\s|$)",
         startupBlockers: [
           {
             pattern: "Warning: No models available",
             message:
               "Pi has no models configured. Configure a provider via `/login` or set DEEPSEEK_API_KEY / GITHUB_TOKEN before routing through clisbot.",
           },
           {
             pattern: "tmux extended-keys is off",
             message:
               "Pi requires tmux extended-keys support. Add `set -g extended-keys on` to ~/.tmux.conf and restart tmux.",
           },
         ],
         promptSubmitDelayMs: 150,
         newSessionCommand: "/new",
         sessionId: {
           create: {
             mode: "explicit",
             args: ["--session", "{sessionId}"],
           },
           capture: {
             mode: "off",
             statusCommand: "/status",
             pattern: SESSION_ID_PATTERN,
             timeoutMs: 5000,
             pollIntervalMs: 250,
           },
           resume: {
             mode: "command",
             args: ["--resume", "{sessionId}", "--dangerously-skip-permissions"],
           },
         },
       },

       Note: pi uses --session (not --session-id like claude). Verify this against RESEARCH.md.

    3. In inferAgentCliToolId (after the gemini case at line ~252, before `return null`):
       if (trimmed === "pi") {
         return "pi"
       }

    Run RED: bun test test/runner-service.integration.test.ts (new pi tests must fail before implementation).
    Run GREEN: implement above changes, then bun test test/runner-service.integration.test.ts must pass.
    Run full suite: bun test to confirm no regressions.
  </action>
  <verify>
    <automated>bun test test/runner-service.integration.test.ts</automated>
  </verify>
  <done>
    All 12 pi template tests pass. SUPPORTED_AGENT_CLI_TOOLS includes "pi". inferAgentCliToolId("pi") returns "pi".
    buildRunnerFromToolTemplate("pi", ...) produces args: ["--dangerously-skip-permissions"] and resume.args: ["--resume", "{sessionId}", "--dangerously-skip-permissions"].
    bunx tsc --noEmit exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config → agent-tool-presets | Operator sets cli: "pi" in config; template values are code-controlled, not operator-controlled |
| template → tmux command | startupOptions and args flow into the tmux spawn command; values are hardcoded in source, not interpolated from user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | startupOptions in template | accept | Values are hardcoded in source (["--dangerously-skip-permissions"]); operator cannot inject via config at this layer |
| T-02-02 | Information Disclosure | startupBlockers message text | accept | Messages are operator-facing diagnostics only; no secrets or PII in message strings |
| T-02-03 | Denial of Service | startupReadyPattern regex | accept | Pattern is hardcoded in source, not user-supplied; no ReDoS surface |
</threat_model>

<verification>
After task completion:
- bun test test/runner-service.integration.test.ts — all pi template tests pass
- bunx tsc --noEmit — exits 0, no new type errors
- grep -n '"pi"' src/config/runtime/agent-tool-presets.ts — shows entries in SUPPORTED_AGENT_CLI_TOOLS, DEFAULT_AGENT_TOOL_TEMPLATES key, and inferAgentCliToolId
</verification>

<success_criteria>
- SUPPORTED_AGENT_CLI_TOOLS = ["codex", "claude", "gemini", "pi"] as const
- DEFAULT_AGENT_TOOL_TEMPLATES["pi"] exists with all AgentToolTemplate fields populated
- sessionId.create.mode === "explicit", args: ["--session", "{sessionId}"]
- sessionId.capture.mode === "off"
- sessionId.resume.mode === "command"
- startupReadyPattern: "(?:^|\\s)escape\\s+interrupt(?:\\s|$)"
- startupBlockers declared for "Warning: No models available" and "tmux extended-keys is off"
- inferAgentCliToolId("pi") returns "pi"
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create .planning/phases/02-runner-session-config/02-01-SUMMARY.md
</output>
