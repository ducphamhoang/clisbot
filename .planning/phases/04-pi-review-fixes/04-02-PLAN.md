---
phase: 04-pi-review-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FIX-07

must_haves:
  truths:
    - "REQUIREMENTS.md SESSION-03 documents --resume as the pi session resume flag"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected SESSION-03 documentation"
      contains: "--resume {uuid}"
  key_links:
    - from: ".planning/REQUIREMENTS.md SESSION-03"
      to: "src/config/runtime/agent-tool-presets.ts pi template resume.args"
      via: "documentation matches implementation"
      pattern: "--resume"
---

<objective>
Correct a one-line documentation error in REQUIREMENTS.md: SESSION-03 currently states `--session {uuid}` as the pi session resume flag, but the implementation correctly uses `--resume {sessionId}`. This is a doc-only fix with zero code changes.

Purpose: Keep REQUIREMENTS.md truthful to the actual pi CLI semantics (`--session` creates, `--resume` resumes — same pattern as claude).
Output: REQUIREMENTS.md line 15 updated. No source files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-pi-review-fixes/04-CONTEXT.md

<interfaces>
<!-- Current state of the line to change -->

From .planning/REQUIREMENTS.md line 15:
```
- [ ] **SESSION-03:** Pi session resume passes `--session {uuid}` plus startup flags
```

Must become:
```
- [ ] **SESSION-03:** Pi session resume passes `--resume {uuid}` plus startup flags
```

Implementation evidence in src/config/runtime/agent-tool-presets.ts (pi template):
```typescript
resume: {
  mode: "command",
  args: ["--resume", "{sessionId}", "--dangerously-skip-permissions"],
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SESSION-03 resume flag in REQUIREMENTS.md (Fix 7)</name>
  <read_first>
    .planning/REQUIREMENTS.md (lines 13–20 — SESSION-03 entry)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
In `.planning/REQUIREMENTS.md`, find line 15:
```
- [ ] **SESSION-03:** Pi session resume passes `--session {uuid}` plus startup flags
```

Change `--session {uuid}` to `--resume {uuid}`:
```
- [ ] **SESSION-03:** Pi session resume passes `--resume {uuid}` plus startup flags
```

This is a single string substitution. No other lines in REQUIREMENTS.md change.
  </action>
  <verify>
    <automated>grep "SESSION-03" /home/brewuser/clisbot/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - `grep "SESSION-03" .planning/REQUIREMENTS.md` outputs a line containing `--resume {uuid}`
    - `grep "SESSION-03" .planning/REQUIREMENTS.md` does NOT output a line containing `--session {uuid}`
  </done>
</task>

</tasks>

<acceptance_criteria>
- `grep "SESSION-03" .planning/REQUIREMENTS.md` contains `--resume {uuid}` and NOT `--session {uuid}`
- No source files are modified (only `.planning/REQUIREMENTS.md`)
- `bun run check` exits 0
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator | Operators read REQUIREMENTS.md to understand pi CLI flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | REQUIREMENTS.md SESSION-03 | mitigate | Correcting `--session` to `--resume` prevents operators from using the wrong flag when manually testing pi resume |
</threat_model>

<verification>
```bash
grep "SESSION-03" .planning/REQUIREMENTS.md
# Expected: line contains --resume {uuid}
bun run check
```
</verification>

<success_criteria>
- Fix 7: SESSION-03 documents `--resume {uuid}` (not `--session {uuid}`)
- Doc matches implementation in agent-tool-presets.ts pi resume.args
</success_criteria>

<output>
After completion, create `.planning/phases/04-pi-review-fixes/04-02-SUMMARY.md`
</output>
