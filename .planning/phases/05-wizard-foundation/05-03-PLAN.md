---
phase: 05-wizard-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01-PLAN.md
  - 05-02-PLAN.md
files_modified: []
autonomous: true
requirements:
  - FOUND-06

must_haves:
  truths:
    - "bun run check passes (typecheck + full test suite green)"
    - "package.json dependencies section is identical to pre-phase state (no new npm packages)"
    - "src/control/setup/setup-wizard-utils.ts imports only from node: stdlib and local src/ paths"
  artifacts:
    - path: "src/control/setup/setup-wizard-utils.ts"
      provides: "Four wizard utility exports (verified clean)"
      exports:
        - ensureTTY
        - ensureDaemonNotRunning
        - writeEditableConfigAtomic
        - withWizardCleanup
    - path: "test/setup-wizard-utils.test.ts"
      provides: "Passing test suite for all four utilities"
  key_links:
    - from: "src/control/setup/setup-wizard-utils.ts"
      to: "node: stdlib only + local src/"
      via: "import statements"
      pattern: "from 'node:|from '../../"
---

<objective>
Phase gate: verify the full FOUND-06 constraint (no new npm dependencies), confirm TypeScript is clean, and run the complete test suite. This plan does not create or modify any source files — it is a verification checkpoint only.

Purpose: Ensure the phase ships in a known-good state before the executor creates the phase SUMMARY. Prevents FOUND-06 violations from slipping through (no new packages introduced by setup-wizard-utils.ts).

Output: A clean `bun run check` result and documented verification of the no-new-deps constraint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-wizard-foundation/05-CONTEXT.md
@.planning/phases/05-wizard-foundation/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify FOUND-06 no-new-deps constraint and run full check gate</name>
  <files></files>

  <read_first>
    - src/control/setup/setup-wizard-utils.ts — scan all import statements to confirm only node: stdlib and local src/ paths; no npm package names
    - package.json — capture the dependencies and devDependencies keys to confirm nothing was added
  </read_first>

  <action>
    Run these verification commands in sequence:

    1. **Check imports in setup-wizard-utils.ts for npm package names:**
       ```bash
       grep "^import" src/control/setup/setup-wizard-utils.ts
       ```
       Every import must start with either:
       - `'node:` (stdlib)
       - `'../../` or `'../` (local src/ path)
       Any import that starts with a bare package name (e.g., `'zod'`, `'chalk'`, `'inquirer'`) is a FOUND-06 violation — fix by removing the dependency.

    2. **Confirm package.json is unchanged:**
       ```bash
       git diff package.json
       ```
       Expected: empty output (no diff). If package.json shows new entries in dependencies or devDependencies, remove them — only node stdlib and local imports are permitted.

    3. **Run targeted wizard tests:**
       ```bash
       bun test test/setup-wizard-utils.test.ts
       ```
       All tests must pass. If any fail, fix the implementation in Plan 02's file before this plan's gate passes.

    4. **Run full check gate:**
       ```bash
       bun run check
       ```
       Must exit 0. Fixes any typecheck errors found before declaring the phase complete.

    Report the output of each command. If all four pass, the phase is verified.
  </action>

  <verify>
    <automated>bun run check</automated>
  </verify>

  <acceptance_criteria>
    - No npm package imports: `grep "^import" src/control/setup/setup-wizard-utils.ts | grep -v "node:\|'\.\." | wc -l` returns 0
    - package.json unchanged: `git diff package.json` returns empty output
    - Targeted tests pass: `bun test test/setup-wizard-utils.test.ts` exits 0
    - Full check passes: `bun run check` exits 0
    - Only stdlib and local imports: `grep "^import" src/control/setup/setup-wizard-utils.ts` shows only lines matching `from 'node:` or `from '../../` or `from '../`
  </acceptance_criteria>

  <done>
    bun run check exits 0. package.json is unmodified. setup-wizard-utils.ts contains zero npm package imports. Phase 5 is complete and ready for SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate→codebase | bun run check validates the full compiled+tested state of the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | Accidental npm dependency introduced | mitigate | Explicit git diff package.json check; grep on import lines in new file catches violations before phase completion |
| T-05-10 | Denial of Service | Broken typecheck blocks downstream phases | mitigate | bunx tsc --noEmit run in bun run check; must exit 0 before phase is declared complete |
</threat_model>

<verification>
```bash
# Final phase gate
bun run check

# FOUND-06 constraint
git diff package.json
grep "^import" src/control/setup/setup-wizard-utils.ts
```
</verification>

<success_criteria>
- bun run check exits 0 (typecheck + full test suite)
- package.json has no new entries (FOUND-06)
- setup-wizard-utils.ts imports only from node: stdlib and local src/ paths (FOUND-06)
- Phase 5 verified and ready for phase SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/05-wizard-foundation/05-03-SUMMARY.md`

Then create the phase-level summary at `.planning/phases/05-wizard-foundation/05-PHASE-SUMMARY.md` with:
- Files created: src/control/setup/setup-wizard-utils.ts, test/setup-wizard-utils.test.ts
- Requirements delivered: FOUND-01, FOUND-02, FOUND-04, FOUND-05, FOUND-06
- Requirements deferred: FOUND-03 (masked input — Phase 6)
- Key decisions locked: D-01 (writeEditableConfig unmodified), D-08 (withWizardCleanup pattern), D-10 (Bun #21189 mitigation), D-11 (single readline interface per session)
- Integration contract for Phase 6: import { ensureTTY, ensureDaemonNotRunning, writeEditableConfigAtomic, withWizardCleanup } from '../setup/setup-wizard-utils.ts'
</output>
