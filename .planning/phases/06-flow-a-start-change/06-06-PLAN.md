---
phase: 06-flow-a-start-change
plan: 06
type: execute
wave: 3
depends_on:
  - "06-04"
  - "06-05"
files_modified: []
autonomous: false
requirements:
  - CHANWIZ-01
  - CHANWIZ-02
  - CHANWIZ-03
  - CHANWIZ-04
  - CHANWIZ-05
  - START-01

must_haves:
  truths:
    - "Full bun run check passes with no errors (typecheck + full test suite)"
    - "No new npm dependencies were introduced"
    - "CHANWIZ-05 and START-01 integration behaviors are manually confirmed"
    - "All six requirement IDs are satisfied"
  artifacts:
    - path: "src/control/setup/setup-channels.ts"
      provides: "Flow A wizard — complete"
    - path: "src/control/setup/setup-wizard-utils.ts"
      provides: "promptMasked added"
    - path: "src/control/commands/runtime-bootstrap-cli.ts"
      provides: "START-01 warn-and-continue"
    - path: "test/control/setup/setup-channels.test.ts"
      provides: "CHANWIZ-01 through CHANWIZ-05 unit coverage"
    - path: "test/startup-bootstrap.test.ts"
      provides: "START-01 regression coverage"
  key_links:
    - from: "test/startup-bootstrap.test.ts"
      to: "src/control/commands/runtime-bootstrap-cli.ts"
      via: "START-01 warn-and-continue tests"
      pattern: "warns and continues"
---

<objective>
Phase 6 verification gate: run `bun run check` to confirm the full suite passes, verify no new npm deps, and checkpoint for manual integration confirmation of CHANWIZ-05 and START-01 live behaviors.

Purpose: Automated gate ensures typecheck + tests are green before marking Phase 6 complete. Manual checkpoint confirms the two behaviors that require a real terminal (wizard runtime start + start warning).
Output: Green `bun run check`, confirmed Phase 6 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-flow-a-start-change/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification gate</name>

  <read_first>
    - .planning/phases/06-flow-a-start-change/06-VALIDATION.md — sampling rate and per-task verification map
    - package.json — confirm no new entries in dependencies or devDependencies vs Phase 5 baseline
  </read_first>

  <action>
    Run the full verification sequence:

    1. TypeCheck all modified files:
       ```
       bunx tsc --noEmit
       ```
       Must produce zero errors.

    2. Run targeted test files:
       ```
       bun test test/control/setup/setup-channels.test.ts
       bun test test/startup-bootstrap.test.ts
       bun test test/control/setup/setup-wizard-utils.test.ts
       ```
       All must pass.

    3. Run full suite:
       ```
       bun run check
       ```
       Must pass with no failures.

    4. Verify no new npm dependencies:
       ```
       grep -E '"node:|"bun:' package.json
       ```
       No new packages should appear. The grep should show only pre-existing entries.
       Also run: `git diff package.json` — must show no changes.

    5. Verify file line counts are within CLAUDE.md limits:
       ```
       wc -l src/control/setup/setup-channels.ts src/control/setup/setup-wizard-utils.ts src/control/commands/runtime-bootstrap-cli.ts
       ```
       All must be under 700 lines (hard limit).

    6. Confirm all 6 requirement IDs are covered by grepping:
       ```
       grep -l "CHANWIZ\|START-01" .planning/phases/06-flow-a-start-change/06-*-PLAN.md
       ```
       Should show all six plans listed.
  </action>

  <verify>
    <automated>bun run check 2>&1 | tail -5</automated>
  </verify>

  <done>
    - `bun run check` exits 0
    - `bunx tsc --noEmit` exits 0
    - `git diff package.json` is empty (no new deps)
    - `wc -l src/control/setup/setup-channels.ts` <= 700
    - `wc -l src/control/setup/setup-wizard-utils.ts` <= 700
    - `wc -l src/control/commands/runtime-bootstrap-cli.ts` <= 700
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual integration verification — CHANWIZ-05 and START-01 live behaviors</name>

  <what-built>
    Plan 06-05 built the complete Flow A wizard (setup-channels.ts). Plan 06-04 changed start() to warn-and-continue when channels are configured but no agent.

    Two behaviors require live terminal verification because they involve real process spawning (startDetachedRuntime) and terminal masking that unit tests cannot capture.
  </what-built>

  <how-to-verify>
    **Verify CHANWIZ-05 — wizard completes and runtime starts in unrouted mode:**

    Prerequisites: dev environment with at least one channel token available (or use a test token).

    ```bash
    # Run the channel wizard (note: requires clisbot setup channels to be registered in CLI)
    # For Phase 6, the wizard is not yet in the CLI — call it directly:
    bun run start setup channels
    # OR if CLI entry not registered yet, test the module directly:
    bun -e "import('./src/control/setup/setup-channels.ts').then(m => m.runChannelsWizard())"
    ```

    Expected behavior:
    1. Prints "=== clisbot setup channels ==="
    2. For each channel: shows if env var detected or asks "Configure this channel? [Y/n]"
    3. Token prompts show asterisks as you type (not plaintext)
    4. After token entry, review screen shows "REVIEW CHANNEL CONFIGURATION" table
    5. On confirming with 'y', config is written and runtime starts
    6. Success screen shows "Next step: clisbot setup agent"

    ```bash
    # Verify runtime started:
    bun run status
    # Should show runtime running
    ```

    **Verify START-01 — clisbot start warns with channels but no agent:**

    Prerequisites: config with at least one channel enabled (telegram/slack/zalo-bot) but no agent entry.

    ```bash
    # Start with channels-only config:
    bun run start
    # OR: node dist/cli.js start
    ```

    Expected output must include:
    ```
    warning: no agent configured — starting in unrouted mode.
    Run clisbot setup agent to add an AI agent.
    ```

    And the process must NOT exit with a failure banner — it should continue to start the runtime.

    **Verify FOUND-03 — masked input shows asterisks:**

    During the wizard token prompt, type a few characters. You should see `***` (one asterisk per character typed), not the actual token characters.
  </how-to-verify>

  <resume-signal>
    Type "approved" if all three behaviors are confirmed.
    Or describe any issue observed (e.g., "CHANWIZ-05: success screen missing clisbot setup agent text").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → src/ | Full suite validates all modified files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | New npm dep accidentally introduced | mitigate | `git diff package.json` check in Task 1 catches any accidental dep addition. FOUND-06 requirement. |
| T-06-06-02 | Information Disclosure | Token visible in test output | accept | Test tokens are fixtures (e.g., "test-token-1234"), not real credentials. Test output in CI has no sensitive value. |
</threat_model>

<verification>
- `bun run check` exits 0
- `git diff package.json` empty
- Manual checkpoint approved by operator
- All 6 requirement IDs (CHANWIZ-01 through CHANWIZ-05, START-01) confirmed covered
</verification>

<success_criteria>
Full automated suite green. No new dependencies. Operator has manually confirmed CHANWIZ-05 wizard runtime start + success screen, START-01 warn-and-continue, and FOUND-03 asterisk masking in live terminal. Phase 6 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/06-flow-a-start-change/06-06-SUMMARY.md`

Also update:
- `.planning/ROADMAP.md` — mark Phase 6 plans as complete
- `.planning/STATE.md` — advance to Phase 7
- `.planning/REQUIREMENTS.md` — mark CHANWIZ-01 through CHANWIZ-05 and START-01 as complete
</output>
